# Migration `watch-20191023165041`

This migration has been generated by Divyendu Singh at 10/23/2019, 4:50:46 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP INDEX `ClassificationJob.signedUrl` ON `classy-local`.`ClassificationJob`

ALTER TABLE `classy-local`.`ClassificationJob` DROP COLUMN `signedUrl`;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..watch-20191023165041
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,31 @@
+datasource db {
+  provider = "mysql"
+  url      = env("MYSQL_URL")
+}
+
+generator photon {
+  provider      = "photonjs"
+  binaryTargets = ["linux-glibc-libssl1.0.2", "native"]
+}
+
+
+model ClassificationJob {
+  id          Int                    @id
+  zipUrl      String                 @unique
+  zipUploaded Boolean                @default(false)
+  results     ClassificationResult[]
+}
+
+model Algorithm {
+  id   Int    @id
+  name String
+  link String
+}
+
+model ClassificationResult {
+  id            Int       @id
+  algorithm     Algorithm
+  accuracy      Float
+  inferenceTime Float
+  trainingTime  Float
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191023165041)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191023165041'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
